# test01

Harbor Wallet KYC identity verification prototype, built with the T3 stack (Next.js, tRPC, Tailwind).

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) and follow the wallet verification flow.
